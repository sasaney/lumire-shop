# Lumire Shop — V6 (منو، فوتر، لندینگ، ارسال، امنیت)

## منو
- منوی دسته‌بندی کامل در دسکتاپ (دراپ‌داون سلسله‌مراتبی)
- منوی موبایل تمام‌صفحه با زیرمجموعه‌های باز/بسته
- لینک دسته‌ها به `/?category=...`

## فوتر (پنل ادمین → تنظیمات سایت → تب فوتر)
- نام برند، متن درباره، کپی‌رایت
- ستون‌های لینک (افزودن/حذف ستون و لینک)
- تماس (عنوان، متن، تلفن، ایمیل)
- شبکه‌های اجتماعی

## لندینگ (تب لندینگ)
- فعال/غیرفعال و ترتیب بخش‌ها: هیرو، اعتماد، دسته، تخفیف، محصولات، نظرات
- ویرایش نشان‌های اعتماد و نظرات مشتریان
- اسلایدر در تب جدا

## روش‌های ارسال (پنل → روش‌های ارسال)
- نام، توضیح، هزینه، پیش‌کرایه/پس‌کرایه، حداقل/حداکثر روز، فعال، ترتیب
- در فرم محصول: محدود کردن روش‌های مجاز (خالی = همه)
- در چک‌اوت: انتخاب روش + محاسبه هزینه

## امنیت
- JWT با الگوریتم صریح HS256 و عمر ۸ ساعت
- اجباری بودن JWT_SECRET قوی در production
- bcrypt cost 12
- middleware محافظ `/admin` و `/api/admin`
- هدرهای امنیتی (CSP پایه، X-Frame-Options، nosniff، HSTS در prod)
- Rate limit OTP بر اساس IP و ایمیل
- عدم افشای devCode OTP در production
- poweredByHeader غیرفعال

## تست پیشنهادی
1. `npm install && npm run dev`
2. ورود ادمین → تنظیمات سایت (فوتر/لندینگ/اسلایدر)
3. روش‌های ارسال → تعریف/ویرایش
4. محصول → تب ارسال
5. موبایل: منوی همبرگر و دسته‌ها
6. چک‌اوت با روش ارسال
